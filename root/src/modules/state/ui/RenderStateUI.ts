import IRenderStateUI from "../../interfaces/state/ui/IRenderStateUI";


export default class RenderStateUI implements IRenderStateUI {

    public raw: boolean = true;
    public optionsExpanded: boolean = false;
}
